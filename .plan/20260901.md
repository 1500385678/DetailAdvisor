# 2026-09-01 T5 临时执行记录

> 来源:0901 巡检 `.Log/巡检-细部-20260901.md` 最高优建议(0830/0831 T5 连续 2 天推到 0901 的 axe-core 集成)
> 执行时段:2026-09-01 03:30

## 任务

1. 装包:`npm install --save-dev axe-core@4.13.0 @axe-core/react@4.13.0`
2. 写 `app/_components/A11yDevAudit.tsx`(client component,dev-only,console 输出 violation)
3. 改 `app/layout.tsx` server 端条件渲染 `process.env.NODE_ENV === "development" && <A11yDevAudit />`
4. 写 `docs/a11y/axe-core_基线_v0.1.md`
5. 勾上 `项目开发计划.md` §5 Phase 0 第 8 个 checkbox
6. 验证:`npx tsc --noEmit` / `npx eslint .` / `npx next build` 三件套
7. 准备 commit + 推 Gitee / GitHub + 删本 plan

## 设计取舍

- 不用 `@axe-core/react` 的 React 集成 API(React 19 兼容性风险),只装包备用
- 不起 dev server 跑实测(遵循 Mac mini "测完即关"约束),实测留给 Phase 1
- 静态代码 review 推断 4 模块占位页潜在 violation,实测数据待补

## 落地状态

- [x] 1. 装包
- [x] 2. 客户端组件
- [x] 3. layout 改造
- [x] 4. 基线报告
- [x] 5. checkbox
- [x] 6. 三件套验证
- [ ] 7. commit + 推 + 清(本文件)
