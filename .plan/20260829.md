# DetailAdvisor · 2026-08-29 T4 计划(由 03:30 T5 自补)

> **T4 → T5 链条断档第 4 天,本 plan 由 T5 03:30 直接补,基于 02:30 巡检报告 §建议**
> 巡检源: `.Log/巡检-细部-20260829.md` §建议(给 T5 03:30 行动)
> 触发: 0828 巡检高优建议"0829 必须有 1 个 package.json + 空 app/page.tsx 起步"

## 今日目标(1 个小变更)

- [x] **代码资产 0 → 1 起步**:在 `DetailWeb/` 根下初始化 Next.js 14 骨架
  - 命令: `npx create-next-app@latest . --typescript --tailwind --app --eslint --no-src-dir --import-alias "@/*" --use-npm --turbopack`
  - 预期产物: `package.json` / `app/page.tsx` / `app/layout.tsx` / `app/globals.css` / `tailwind.config.ts` / `tsconfig.json` / `next.config.ts` / `next-env.d.ts` / `eslint.config.mjs` / `.gitignore` 增量
  - 验收: `npm run build` 通过(或至少 `npx tsc --noEmit` 不报错);`app/page.tsx` 含默认 home 页
  - 边界: **不**起 dev server(`npm run dev`)持续运行,0829 巡检 §"测试完成后关闭程序"规则
  - 留痕: commit message 含"代码资产 0 → 1 起步(0828 巡检高优建议兑现)"

## 不做什么(本日边界)

- 不引入数据库(Prisma / pg)——Phase 1 后半段
- 不接入飞书 Bot——Phase 0 外部依赖,等飞书 appId
- 不写 Next.js 业务页面——只起骨架,业务 page 留 Phase 1
- 不动文案审查规则种子——v0.1/v0.2/v0.3 已闭环,等词库全量(Marketing 对接)

## 完成后

- 更新 `项目开发计划.md` §5 Phase 0:
  - 新增"初始化 Next.js 14 骨架(2026-08-29 落地,代码资产 0 → 1 起步)"
  - 注: Phase 0 不直接收口(还差 3 项),但代码起步为 Phase 1 MVP 铺路
- git add -A → 推送 Gitee + GitHub
- 删除本临时 plan
