# 2026-09-04 临时 plan · T4 02:30 未起草 · T5 临时决策(显式留痕)

> 模式:**无 plan,临时决策**(0902-0903 连续 2 天模式延续,0904 巡检已确认)
> 决策时间: 2026-09-04 03:30 (Asia/Shanghai)
> 决策依据: `.Log/巡检-细部-20260904.md` §"建议(给 T5 03:30 行动)"

## 目标(单一目标,简洁可入库)

**兑现 0904 巡检"最高优"项(B)**:audit-text API 零依赖增量扩展
- v0.1 错别字 3 条零依赖(纯 regex 匹配,无需 hanlp):
  - R-TYPO-02 多字/漏字/重复字(叠词白名单)
  - R-TYPO-03 标点符号错误(中英文混用)
  - R-TYPO-05 全角/半角混用
- v0.3 语气可读性 2 条零依赖(纯 regex 匹配,无需 LLM):
  - R-TONE-02 否定句否定词置顶(检测"请不要"/"请勿"引导)
  - R-TONE-03 语气一致性(4 类语气词频次统计)

**副目标(同时兑现)**:兑现 0904 巡检"高优"项(D) README.md 增补"无 plan,临时决策"约定

## 范围

- `app/api/audit/text/route.ts`:新增 5 条规则(2 → 7 规则)
- `docs/api/audit-text-v0.1.md`:增量更新规则清单 + 端到端示例
- `docs/审查规则/v0.1_文案审查_错别字_敏感词.md`:变更记录追加 0904
- `docs/审查规则/v0.3_文案审查_语气_可读性.md`:变更记录追加 0904
- `项目开发计划.md` §6 Phase 1 §6 子项"启动 v0.1 增量"勾
- `README.md`:增补"无 plan,临时决策"约定段

## 不做什么

- ❌ 不做 v0.1 错别字 R-TYPO-01(同音字) - 需 hanlp 字典
- ❌ 不做 v0.1 错别字 R-TYPO-04(量词) - 需 LLM
- ❌ 不做 v0.2 品牌词 4 条 - 需品牌词表(Phase 0 外部依赖,0904 起降级到 Phase 1.5)
- ❌ 不做 v0.3 R-TONE-01(二义性) - 需 LLM
- ❌ 不做 v0.3 R-READ-03(信息密度) - 需 LLM
- ❌ 不做 axe-core 真实页面基线 - 需 `npm run dev` + 浏览器 console(下次再做)
- ❌ 不做模块 2 异常场景发现器 - 留下次

## 验证标准

- [ ] `npx tsc --noEmit` 通过
- [ ] `npx eslint .` 通过
- [ ] `npx next build` 通过
- [ ] 至少 8 个 curl 端到端用例全过(GET 元信息 + 5 规则各 1 命中用例 + 1 PASS 用例 + 1 多规则累加用例)
- [ ] `项目开发计划.md` 对应 checkbox 勾
- [ ] `README.md` 增补"无 plan,临时决策"约定
