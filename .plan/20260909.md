# 2026-09-09 T5 临时 plan · R-TYPO-08 增量 · 12 规则收口

> 临时 plan · 当日 03:30 T5 决策依据 · 落地后由 T5 commit 一并清理
> 决策时间: 2026-09-09 03:30 CST · 触发: 03:30 T5 cron

## 今日决策

**增量 R-TYPO-08 广告法极限词零依赖查表**,把 Phase 1 §6 模块 1 从 11 规则扩到 **12 规则收口**,兑现 0908 巡检建议"目标 12+ 规则收口"。

## 选型

| 维度 | 选择 | 理由 |
|------|------|------|
| 主题 | 广告法极限词(非 R-TYPO-01 计划"同音字") | ① 同音字需 hanlp 字典,与 0908 "零依赖纯机检"模式不符;② 极限词属"零依赖静态词表"族,纯字符串查表;③ 11 规则 0.6 → 12 规则 0.7 起步,选最易落地的方向 |
| 词表 | 4 类 33 条 2+ 字词 | absolute 8 / ranking 9 / degree 9 / promise 7;**避免单字"最"/"全"误报** |
| 算法 | 静态词表 + 长词优先 + 区间去重 | 零依赖;`text.indexOf` 循环 + covered[] 区间防子串重复报 |
| 字段 | 5 字段(加 category) | 4 类 category 字段便于客户端差异化提示(法务 vs 营销 vs 金融) |

## 词表(4 类 33 条)

- **absolute 绝对化(8)**: 最佳 / 最好 / 最大 / 最高 / 最优 / 最强 / 最快 / 最便宜
- **ranking 排名/地位(9)**: 第一 / 唯一 / 首选 / 独家 / 顶级 / 顶尖 / 最高级 / 国家级 / 世界级
- **degree 程度极限(9)**: 100% / 百分百 / 百分之百 / 永久 / 永远 / 绝对 / 完全 / 完美 / 万能
- **promise 承诺/保证(7)**: 包过 / 稳赚 / 零风险 / 无风险 / 稳赚不赔 / 无副作用 / 立竿见影

## 文件改动

1. `app/api/audit/text/route.ts`
   - 新增 `Typo08Hit` 接口(5 字段:rule/text/position/match/category)
   - 新增 `TYPO08_MAX = 3` 常量
   - 新增 `ABSOLUTE_WORDS` 静态词表常量(33 条 4 类)
   - 新增 `checkTypo08` 函数(长词优先 + 区间去重)
   - `AuditResponse` 接口加 `typo08_hits: Typo08Hit[]`
   - POST handler 串入 `checkTypo08` + 12 规则累加
   - `RuleSummary` 接口加 `typo08`
   - `buildSummary` 加 R-TYPO-08 段
   - GET `version` 升级到 `+R-TYPO-08`;`rules_implemented` 12 条

2. `docs/api/audit-text-v0.1.md`
   - 标题 + 落地 commit 列表更新
   - §1 范围 9 → 12 规则
   - §2.3 响应加 `typo08_hits` 字段 + summary 11 → 12 段
   - 新增 §3.10 R-TYPO-08 规则细节(词表/算法/示例)
   - 新增 §6.7 0909 T5 验证清单
   - §7 关联文档加 0909 R-TYPO-08 标注
   - §8 变更记录追加 0909 行

3. `docs/审查规则/v0.1_文案审查_错别字_敏感词.md`
   - §3 新增 R-TYPO-08 规则定义(词表/算法/示例/阈值)
   - §10 变更记录追加 0909 行

4. `项目开发计划.md` §6 模块 1
   - 新增 0909 T5 子项勾选 [x]
   - 异常场景发现器备注更新(12 规则已收口,0910+ 可启动)

## 验证

- `npx tsc --noEmit` 通过
- `npx eslint .` 通过
- `npx next build` 通过
- 8 个 curl 端到端用例全过

## 后续(0910+)

- Phase 1 §6 模块 1 **12 规则收口完成**
- 可启动: 异常场景发现器 / A11y 审查 v1 / 飞书 bot 触发接入 / 健康度曲线 / dogfood
- 极限词 v0.2 增量: 接 LLM 二次校验做上下文豁免(对齐 v0.1 §4.3 原则)
