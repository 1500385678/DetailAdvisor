# 文案审查规则 v0.3 · 语气 + 可读性

> DetailAdvisor 模块 1(文案审查器)种子规则 · v0.3 · 2026-08-28
> 来源: `.plan/20260825_细部方法论盘点.md` §三 A 组第 2/4/5 条(二义性 / 句长 / 否定句)
> 前置: v0.1(错别字+敏感词,0826 落地)· v0.2(品牌词,0827 落地)· v0.3 在其之上扩展第 4 类
> 后续: v1.0(完整 4 类规则 + LLM prompt + FastAPI 接口,与 Web App 骨架同步)
> 对接: 项目开发计划.md §3 模块 1(文案细节审查器) + Phase 0 第 3 类规则

---

## 1. 适用范围

| 项 | 说明 |
|---|------|
| 适用 | 用户可见的全部中文文案(页面/落地页/推送/邮件/按钮/错误提示/表单 placeholder/Tooltip) |
| 不适用 | 代码注释、log 文本、API 错误码(技术文本)、英文 i18n(本规则仅中文)、引号/书名号包裹的引用块 |
| 优先级 | **P2** —— 软调,在 v0.1 错别字/敏感词(P0 硬错)、v0.2 品牌词(P1 软错)之后,作为审查器第三遍扫描 |

## 2. 核心原则

> **二义性是文案的头号杀手** —— 同一句让两个人读出两种动作,必是 bug。
> 句长、否定词位置、二义性 → 三个最常见但工程上**最易机检**的语气/可读性指标。
> 软调不阻塞,但报告里高亮 → 让文案 PM 自己判断是否改。

## 3. 语气规则(可机检 + LLM 校验)

### R-TONE-01 · 二义性检测(LLM 主导)

- **规则**: 同一句文案存在 2 种及以上合理解读(动作不同 / 主体不同 / 数量不同)即报 `[ambig]`
- **示例**:
  - ❌ "请在 24 小时内确认" —— 24h 是时间窗还是动作?动作是"在 24h 内"还是"在 24h 之前"?
  - ❌ "把按钮改成红色" —— 改"按钮"还是改"按钮"色?改"按钮"为"红底"还是"红字"?
  - ✅ "请在 24 小时内**完成确认**" / "请于 2026-08-29 前确认"
  - ✅ "把主按钮**底色**改为红色"
- **机检方式**: LLM 二次校验(让 LLM 列出 2-3 种解读,若 ≥ 2 种都合理则报)
- **阈值**: warn 级,不阻塞,报告高亮

### R-TONE-02 · 否定句否定词置顶

- **规则**: 中文否定句的否定词("不/别/未/没有/无")应紧贴动词/被修饰语,避免"请不要...请不要"句式
- **示例**:
  - ⚠️ "**请不要**重复点击" → 优 "**不要**重复点击"(省 1 字,主语显性)
  - ⚠️ "**请不要**在未登录时使用" → 优 "**未登录时**请不要使用"(状态前置)
  - ✅ "未付款前不可使用"
- **机检方式**: regex 检测"请不要" / "请勿" 引导的句式 + LLM 二次校验
- **阈值**: warn 级,只提示不强制

### R-TONE-03 · 语气一致性(品牌调性匹配)

- **规则**: 同一页面/落地页内,语气词("请/麻烦/建议/温馨提示")出现频次应统一,避免前后不一
- **示例**:
  - ⚠️ 同一落地页: "**请**填写用户名" + "**麻烦**输入密码" + "**温馨提示**:完成注册" —— 三种语气混用
  - ✅ 统一为: "**请**填写用户名" + "**请**输入密码" + "**请**完成注册"
- **机检方式**: regex 统计 4 类语气词频次,占比最大 < 70% 即报
- **阈值**: warn 级,提示"主语气词建议统一为 X(占 65%)"

### R-TONE-04 · 感叹号密度(零依赖纯机检)

- **规则**: 全篇感叹号(中英"!"/"！")数量 / 句子数 > 30% 即报,提示"文案风格过激,建议降低语气强度"
- **示例**:
  - ❌ "快来!超棒!新功能!上线啦!" → 4 句全感叹号,密度 100% > 30% → warn
  - ⚠️ "新功能已上线!请立即体验。" → 1 感叹号 / 2 句 = 50% > 30% → warn(连续 2 句 1 感叹号即触发)
  - ✅ "新功能已上线。请立即体验。" → 0 感叹号,密度 0% → pass
- **机检方式**: EXCLAM_RE `/[!！]/g` 扫中英感叹号数 + splitSentences 句子数 → 计算 ratio → > TONE04_THRESHOLD (0.3) 即报
- **阈值**: warn 级,全篇级只报 1 hit(单条文案上限 2 分)
- **机检友好**: 零依赖纯 regex,EXCLAM_RE 单 regex 覆盖中英感叹号,无需白名单(疑问/感叹语气词白名单已在 R-READ-03 末"!"末"?"处处理)
- **执行顺序**: 与 R-TONE-01/02/03 同级,第三遍扫(软调区)

## 4. 可读性规则(纯机检)

### R-READ-01 · 句长上限(移动端 28 / 桌面端 40)

- **规则**: 移动端文案单句长度(以标点切分后的最长句)上限 28 字;桌面端放宽到 40 字
- **示例**:
  - ❌ 移动端: "**您已成功提交本次申请的详细资料,我们将在 24 小时内由专属顾问与您取得联系以确认后续流程**"(49 字)
  - ✅ 移动端: "**已成功提交资料,24h 内专属顾问将联系您**"(18 字)
  - ❌ 桌面端: 单句 > 40 字
- **机检方式**: 按"。"/"!"/"?"切句,统计最长句 Unicode 字符数
- **阈值**: 超 1 字 1 分,单条文案上限 5 分

### R-READ-02 · 句子开头不堆叠连词

- **规则**: 句子开头禁止堆叠连词("而且...并且...所以...因此"等)
- **示例**:
  - ❌ "**而且并且**功能已上线" → 拆为"**功能已上线**"
  - ❌ "**所以因此**请重新登录"
- **机检方式**: regex 检测"句首连词堆叠"
- **阈值**: 命中即报,白名单豁免(引文/代码块)

### R-READ-03 · 句末标点规范(零依赖纯机检,0908 命名复用)

- **规则**: 陈述句末应以"。"(中文)或"."(英文)或","/";等分隔标点结尾;不应以"!"或"?"结尾(疑问/感叹句除外)或完全无标点结尾
- **示例**:
  - ❌ "资料已提交!顾问将联系您!" → 2 句陈述句末"!" → inconsistent-ending × 2
  - ❌ "请确认信息后再次提交" → 句末"提交"无标点 → missing-punct
  - ✅ "请确认订单。" → 末"。"合法
  - ✅ "Hello world. Please confirm." → 英文双句末"."合法
  - ✅ "你确认吗?请回复" → "你确认吗?"含"吗"疑问词豁免 + "请回复" missing-punct
- **机检方式**: **独立 regex** SENTENCE_WITH_ENDING_RE `/[^。！？!?;；]*[。！？!?;；]?/g` 保留句末标点扫描 + 中英合法标点集判定(CJK_STATEMENT_ENDINGS / LATIN_STATEMENT_ENDINGS)+ 疑问/感叹语气词白名单 `INTERROGATIVE_PARTICLES = /[吗呢吧呀啊哦哇哎]/` 豁免
- **阈值**: 命中即报(warn 级),issue 双分类 `missing-punct`(无标点)/ `inconsistent-ending`(陈述句末"!"/"?"),每命中 1 句 1 分,单条上限 3 分
- **机检友好**: 零依赖纯 regex,独立 SENTENCE_WITH_ENDING_RE 解决 splitSentences 剥标点问题(切句标点集 [。!?！？;；] 与 SENTENCE_SPLIT_RE 一致,字符类 `*` 匹配"非切句标点任意字符"避免吃"!""?")
- **命名说明**: 本规则 ID 沿用 v0.3 种子 R-READ-03(原 LLM 版"信息密度"留 R-READ-03-LLM 标注);0908 T5 落地的零依赖"句末标点规范"是 v0.3 R-READ-03 ID 的零依赖邻居规则,语义不同但同 ID,见 meta.rules_skipped 标注

### R-READ-03 · 信息密度(动词+名词配对)

- **规则**: 句子中"动词 + 抽象名词"配对不超过 3 组,避免抽象堆砌
- **示例**:
  - ❌ "**优化**了**流程**/**提升**了**效率**/**增强了**体验"—— 3 组动词+抽象名词堆叠
  - ✅ "**优化**了登录**流程**"—— 1 组
- **机检方式**: LLM 提取"动词+抽象名词"配对,数量 > 3 即报
- **阈值**: warn 级,提示"信息密度过高,建议拆句"

## 5. 附录 A · DetailAdvisor 自检示例(自身产品文案)

> 真实场景需 Marketing 拉品牌调性表后填充(下阶段任务)。
> 本附录只列自身产品页文案,供规则自检。

| 场景 | 原文案 | 命中规则 | 建议改写 |
|------|--------|----------|----------|
| 落地页主标题 | "请在 24 小时内确认你的账户详情" | R-TONE-01 (二义) | "请于 2026-08-29 前确认账户详情" |
| 推送通知 | "请您不要在未登录状态下使用此功能" | R-TONE-02 (否定置顶) | "未登录时,请不要使用此功能" |
| 错误提示 | "请检查您输入的信息,麻烦确认无误后再次提交" | R-TONE-03 (语气混) | "请检查信息后再次提交" |
| 按钮文案 | "您已成功提交本次申请的详细资料,我们将在 24 小时内由专属顾问与您取得联系以确认后续流程" | R-READ-01 (句长 49) | "资料已提交,24h 内顾问将联系您" |
| 工具提示 | "而且并且此功能尚在测试阶段,可能会影响数据" | R-READ-02 (连词堆叠) | "此功能尚在测试阶段,可能影响数据" |

## 6. 附录 B · v0.1 / v0.2 / v0.3 优先级关系

| 规则版本 | 检查类别 | 优先级 | 检查时机 | 阻塞性 |
|----------|----------|--------|----------|--------|
| v0.1 (P0) | 错别字 + 敏感词 | **硬错** | 第一遍扫 | 阻塞(HARD_BLOCK) |
| v0.2 (P1) | 品牌词 | **软错** | 第二遍扫 | 报告高亮(不阻塞) |
| **v0.3 (P2)** | **语气 + 可读性** | **软调** | **第三遍扫** | **报告提示(不阻塞)** |

**执行顺序**:v0.1 → v0.2 → v0.3,后一步看到前一步的 `[err]` 标记时**跳过**该词组(避免误报)。
v0.3 不做硬错判断,只生成 `[warn]` 与 `[tip]` 两类信号,合并入报告"软调区"。

## 7. 验证标准(自检 3 项)

- [x] 包含 3-4 条机检级规则(本规则含 6 条:TONE-01/02/03 + READ-01/02/03)
- [x] 附录 A 自身产品文案自检示例不为空(5 行,覆盖全部 6 条规则)
- [x] 与 v0.1 / v0.2 优先级关系明确(附录 B 表格 + 执行顺序说明)

## 8. 不做什么(明确边界)

- ❌ 不做情感分析(积极/消极情绪评分)—— LLM 成本高且工程化价值弱,留 v1.0+
- ❌ 不做 A/B 文案对比(双版本对照)—— 属测试方法论,非审查器职责
- ❌ 不做多语种语气(英文 / 日文)—— 本规则仅中文
- ❌ 不做语音节奏/韵律分析 —— 留 v1.0+ 与可读性 LLM 联合判断
- ❌ 不做"营销话术 vs 严谨措辞"风格切换 —— 留 Phase 2 智能升级阶段

## 9. 变更记录

| 日期 | 变更 | 触发 |
|------|------|------|
| 2026-08-28 | v0.3 起草:语气 3 条(TONE-01/02/03) + 可读性 3 条(READ-01/02/03) + 自身文案自检附录 + 优先级附录 B | T5 03:30 cron |
| 2026-09-02 | R-READ-01(句长上限,移动端 28 / 桌面端 40)落地 `app/api/audit/text/route.ts`,Phase 1 §6 模块 1 启动 commit | 03:30 T5 cron |
| 2026-09-03 | R-READ-02(句首连词堆叠,24 词连词词典)落地,0903 巡检"高优"项 100% 兑现 | 03:30 T5 cron |
| 2026-09-04 | R-TONE-02(否定句否定词置顶,检测"请不要"/"请勿"引导,regex 零依赖)+ R-TONE-03(语气一致性,4 类语气词[请/麻烦/建议/温馨提示]占比 ≥ 70%,regex 零依赖)2 条零依赖纯机检规则在 `app/api/audit/text/route.ts` 实现;**R-TONE-01(二义性,需 LLM 二次校验) + R-READ-03(信息密度,需 LLM 动词+抽象名词提取)仍需 LLM API,留 v0.3 API 后续增量**;至此 v0.3 6 条规则中 4 条已落地(R-READ-01/02 + R-TONE-02/03),剩余 2 条均需 LLM | 03:30 T5 cron |
| 2026-09-08 | **R-READ-03 句末标点规范**(陈述句末"!"/"?"或无标点,即报,**沿用 v0.3 种子 R-READ-03 ID 但语义为"句末标点规范",原 LLM 版留 R-READ-03-LLM 标注**)+ **R-TONE-04 感叹号密度**(全篇感叹号数 / 句子数 > 30% 即报,零依赖纯机检,0908 命名复用 v0.3 序列 TONE-04)2 条零依赖纯机检规则在 `app/api/audit/text/route.ts` 实现;**v0.3 6 条规则中 5 条已落地**(R-READ-01/02/03 + R-TONE-02/03/04),剩余 1 条 R-TONE-01 二义性仍需 LLM;**恢复 0907 T5 首次断档后连续节奏**;至此 Phase 1 §6 模块 1 从 9 规则 → 11 规则,代码资产 0.5 → 0.6 起步;**0829 起 `.plan/` 漂移模式第 9 天延续**(0902-0903-0904-0905-0906-0907-0908 连续 7 天"无 plan,临时决策"模式,0907 T5 首次断档,0908 T5 恢复);0825-0908 共 15 个 T4/T5 周期;**目标 12+ 规则收口 Phase 1 §6 模块 1,后续可再扩 v0.1 错别字 1-2 条 R-TYPO-08 同音字词表(零依赖 Top 50 子集)** | 03:30 T5 cron |
